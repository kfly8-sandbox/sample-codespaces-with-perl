{
  "name": "Perl Development Environment",
  "image": "perl:5.42.0-slim",
  "postCreateCommand": "apt-get update && apt-get install -y locales && locale-gen en_US.UTF-8 && cpanm Carton && carton install",
  "forwardPorts": [3000],
  "customizations": {
    "vscode": {
      "extensions": [
        "richterger.perl"
      ]
    }
  },
  "remoteEnv": {
    "LANG": "en_US.UTF-8",
    "LC_ALL": "en_US.UTF-8"
  }
}
